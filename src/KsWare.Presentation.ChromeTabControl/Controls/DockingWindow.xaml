<Window 
	x:Class="KsWare.Presentation.Controls.DockingWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:ct="clr-namespace:ChromeTabs;assembly=ChromeTabs"
	Title="DockingWindow"
	Height="400"
	Width="400"
	Loaded="Window_Loaded">
    <Window.Resources>
	    <!-- TODO do we need DataTemplates? if yes try use other logic -->
	    <!-- <DataTemplate DataType="{x:Type vm:Class1TabItemVM}"> -->
        <!--     <usercontrols:UserControl1 /> -->
        <!-- </DataTemplate> -->
        <!-- <DataTemplate DataType="{x:Type vm:Class2TabItemVM}"> -->
        <!--     <usercontrols:UserControl2 /> -->
        <!-- </DataTemplate> -->
        <!-- <DataTemplate DataType="{x:Type vm:Class3TabItemVM}"> -->
        <!--     <usercontrols:UserControl3 /> -->
        <!-- </DataTemplate> -->
        <Storyboard x:Key="FadeInContentAnim">
            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Visibility"
                                           Storyboard.TargetName="ContentControl">
                <DiscreteObjectKeyFrame Value="{x:Static Visibility.Visible}"
                                        KeyTime="0:0:0.1" />
            </ObjectAnimationUsingKeyFrames>
        </Storyboard>
    </Window.Resources>
    <Grid>
        <ContentControl x:Name="ContentControl"
                        Visibility="Collapsed"
                        Content="{Binding}" />
    </Grid>
</Window>
